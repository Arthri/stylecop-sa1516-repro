extern alias DI;
global using System.IO.Compression;
using System.ComponentModel;

internal static class Z
{
    internal static void X()
    {
        Console.WriteLine(default(ZipArchive));
        Console.WriteLine(default(LocalizableAttribute));
    }
}
